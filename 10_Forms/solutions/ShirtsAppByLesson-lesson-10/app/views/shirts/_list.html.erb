<% @shirts.each do |shirt| %>
  <section>
    <header><%= shirt.name %></header>
    <p><%= shirt.description %></p>
    <%= image_tag shirt.image %>
    <%= link_to "Show", shirt %>
  </section>
<% end %>